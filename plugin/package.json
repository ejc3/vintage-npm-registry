{
  "name": "verdaccio-plugin-vintage",
  "version": "1.0.0",
  "description": "Verdaccio filter plugin that acts as a time machine for npm packages",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run && cd .. && ./test-e2e.sh",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "verdaccio",
    "plugin",
    "filter",
    "npm",
    "registry"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "semver": "^7.5.4"
  },
  "peerDependencies": {
    "@verdaccio/types": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/semver": "^7.5.6",
    "@verdaccio/types": "^10.8.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
